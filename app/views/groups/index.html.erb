<div class="container">
  <div class="form">
    <% current_user.groups.each do |group| %>
      <div class="group">
        <%= link_to group_path(group) do %>
          <%= group.name %>
        <% end %>
       </div>
    <% end %>

  </div>
</div>