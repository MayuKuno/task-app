<div class="container">
  <div class="temp">
    <% if logged_in? %>
      <p>
        TOP
      </p>
      <%= link_to "Logout", logout_path, method: :delete %>
      <%= link_to "グループ一覧", groups_path %>
      <% if current_user.admin? %>
        <%= link_to "ユーザー一覧", admin_users_path %>
      <% end %>
    <div class="new">
      <%= link_to new_task_path do %>
        <%= image_tag 'plus.png' %>
      <% end %>
    </div>

    <div class="search">
    <%= form_with(local: true, method: :get, class: "search-form") do |form| %>
      <%= form.text_field :keyword, placeholder: "タスクを検索する", class: "search-input" %>
    <% end %>
    </div>


    <table class="tasks table-sortable">
      <thead>
        <tr>
          <th>Label</th>
          <th id="taskname"><%= sortable "taskname", "Name of Task" %></th>
          <th>Status</th>
          <th><%= sortable "deadline", "Deadline" %></th>
          <th><%= sortable "created_at", "Created_at" %></th>
          <th>Manage</th>
        </tr>
      </thead>
    <% @tasks.each do |task| %>
      <tbody>
        <%# <tr>       %>
        <%= content_tag "tr", class: "item", data: { model_name: task.class.name.underscore, update_url: task_sort_path(task)} do %>
          <td>
            <% task.labels.each do |label| %>
              <%= label.color %>
            <% end %>
          </td>
          <td><%= task.taskname %></td>
          <td><%= task.status %></td>
          <td><%= task.deadline %></td>
          <td><%= task.created_at %></td>
          <td>
            <%= link_to 'Edit', "/tasks/#{task.id}/edit", method: :get %>
            <%= link_to 'Detail', task_path(task.id) %>
            <%= link_to 'Delete', task_path(task.id), method: :delete, id: :delete %>
          </td>
        <%# </tr> %>
        <% end %>
      </tbody>
    <% end %>
    </table>
    <%= link_to 'エクスポート', tasks_path(format: :csv) %>


    <%= form_tag import_tasks_path, multipart: true do %>
      <%= file_field_tag :file %>
      <%= submit_tag "インポート" %>
    <% end %>

  <%= link_to 'グループ作成', new_group_path %>

    <%= render 'layouts/calendar'%>

    <%= paginate(@tasks) %>
    <% else %>
      <div class="before">
        <section class="top">
          <div class="message">Manage your day with <br><span>Task App</span></div>
          <div class="btns">
            <%= link_to "Login", login_path, method: :get, class: :btn %>
            <%= link_to "Signup", new_admin_user_path, class: :btn %>
          </div>
        </section>


        <section class="middle">
          <div class="description">
            <div class="message">
              With Task App, <br>
              Priotize your tasks, and<br>
              Save your time<br>
            </div>
          </div>
          <div class="video">
            <%= image_tag 'app.png' , class: :app %>
            <div class="app">
              <%= image_tag  'video.png', class: :video %>
           </div>
          </div>
         
        </section>


        <section class="bottom">
        </section>
        <%= render 'layouts/footer'%>
      </div>
    <% end %>
  </div>
</div>
